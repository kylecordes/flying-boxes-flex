<?xml version="1.0" encoding="utf-8"?>
<mx:Application xmlns:mx="http://www.adobe.com/2006/mxml" layout="absolute"
	xmlns:fly="fly.*">
	<mx:Button x="31" y="77" label="Button" click="populate()"/>
	<mx:Label id="lab1" x="31" y="10" text="Flying boxes demo"/>
	<mx:Label id="lab2" x="31" y="36" text="Drag and drop the work orders."/>
	
	<mx:Script>
	
		<![CDATA[
import fly.Slots;

private function populate(): void {
	var slots:Slots = new Slots(this);
	slots.setupSlots(10, 100);
	
	/*
	List<WorkOrder> wos = new ArrayList<WorkOrder>();
			for (int i = 0; i < 30; i++) {
				String product = "Delivery for " + RandomData.makeName();
				int urgency = (int) (System.nanoTime() % 4 + 1);
				WorkOrder wo = new WorkOrder(i, RandomData.makeAddress(), product,
						urgency);
				wos.add(wo);
			}
	
			orderingWidget.load(wos);
			*/
}
			
		]]>
	</mx:Script>
	<fly:OrderingWidget x="166" y="77" height="277" width="311"/>
</mx:Application>
